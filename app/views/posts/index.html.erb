<h1>List of all Posts by <%= @user.id %></h1>

<h3><%= @user.name%></h3>
<img src= '<%= @user.photo %>' alt='photo comes here' width="100" height="100" />


<p>Complete list of all posts made by the user</p>
<p>
  <% @posts = Post.where(author_id: @user.id) %> 
</p>
<ul>
    <% @posts.each do |post| %>
      <li>
        <h4> <%= post[:title] %> </h4>
        <p> <%= post[:text] %> </p>
      </li>
    <% end %>
</ul>
